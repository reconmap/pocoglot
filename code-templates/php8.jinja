<?php declare(strict_types=1);
{% if "namespace" in poco %}
namespace {{ poco.namespace }};
{%- endif %}
{% if "description" in poco %}
/**
 * {{ poco.description }}
 */
{%- endif %}
class {{ poco.name }} {
{% for prop in poco.props %}
	public {{ prop.type }} ${{ prop.name }}{% if "default" in prop %} = {{ prop.default }}{% endif %};
{%- endfor %}
}

